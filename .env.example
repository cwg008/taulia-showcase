# Taulia Prototype Showcase - Environment Configuration

# Server
NODE_ENV=development
PORT=3001
HOST=0.0.0.0

# Database (SQLite for development)
DB_CLIENT=sqlite3
DB_FILE=dev.sqlite3

# Database (PostgreSQL for production - uncomment and fill in)
# DB_CLIENT=pg
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=taulia
# DB_PASSWORD=your-password
# DB_NAME=taulia_showcase
# DATABASE_URL=postgresql://user:password@host:5432/taulia_showcase

# JWT Authentication
# IMPORTANT: Generate a strong secret for production:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=dev-secret-change-in-production
JWT_EXPIRY=24h

# Email (for admin invitations)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@taulia.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@taulia.com

# Client URL
CLIENT_URL=http://localhost:5173
