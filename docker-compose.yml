version: '3.8'

services:
  web:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./server/uploads:/app/server/uploads
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
    restart: unless-stopped
